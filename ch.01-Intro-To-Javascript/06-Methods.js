// Methods are functions that belong to an object. We can perform actions on objects using methods.
console.log(" Methods in JavaScript ".trim()); //The trim() method will removes the whitespace from both ends of a string.
console.log("hello universe".toUpperCase());  //The toUpperCase() method will transform the string to uppercase.
console.log("hello universe".toLowerCase());  //The toLowerCase() method will transform the string to lowercase.