// This is example of function parameter and argument.
// In the functions the Parameter is the variable that is declared in the function definition.
// In the function call the argument is the actual value that is passed to the function when it is called.

// In this Function the 👇👇 message keyword is the parameter.
function greetingMessage(message){
    console.log(message);
}
greetingMessage("Hello Universe!"); // Hello Universe!
// And here the 👆👆 "Hello Universe!" is the argument.