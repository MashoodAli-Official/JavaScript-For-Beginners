// In var Hoisting the variable is hoisted to the top of the function scope and variable declared on the bottom of the function. 
// By using the Var keyword, error will be undefined due to var scope.

console.log(myNewVariable); // undefined
var myNewVariable = 1;