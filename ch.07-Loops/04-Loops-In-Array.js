// Loops through an array of objects

const studentsName = ['Harry', 'Ron', 'Hermione', 'Draco', 'Neville', 'Luna', 'Ginny', 'Fred', 'George'];
for (let a = 0; a<studentsName.length; a++){
    console.log(studentsName[a]);
};