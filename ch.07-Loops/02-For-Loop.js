// For loop in javascript is used to iterate over a sequence of values.
// Suyntax: for (initialization; condition; increment/decrement) { code block }

for(let num = 0; num <10; num++){
    console.log(num);
}